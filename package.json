{
  "name": "blog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "SKIP_FETCH=1 nuxt dev",
    "build": "node scripts/fetch-content.mjs && nuxt build",
    "generate": "node scripts/fetch-content.mjs && nuxt generate",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.21.4",
    "vue": "^3.5.33"
  },
  "devDependencies": {
    "@nuxtjs/mdc": "^0.16.0"
  }
}
