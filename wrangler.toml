name = "blog"
main = ".output/server/index.mjs"
compatibility_date = "2026-05-04"
# 启用 Node.js 兼容模式，支持 node:events 等内置模块
compatibility_flags = ["nodejs_compat"]

# 静态资源配置（Workers Assets）
[assets]
directory = ".output/public"
